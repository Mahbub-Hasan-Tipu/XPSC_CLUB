#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    string s;
    cin >> s;

    string flag = "None";

    for (int i = 0; i < s.size(); i++)
    {
    }

    if (flag == "None")
    {
        cout << flag;
    }
    else
    {
        cout << flag;
    }
    // cout << s.size();

    return 0;
}